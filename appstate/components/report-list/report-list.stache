<ul>
	{{#each reports}}
	<li class="report">
		<input type='checkbox' can-value="{selected}" />
		<span class="name">{{name}}</span>
		<div class="flags">{{formatFlags .}}</div>
	</li>
	{{/each}}
</ul>